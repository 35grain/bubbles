{{#if isSecondary}}
<div class="container">
        <div class="links">
            {{#foreach navigation}}
            <a href="{{url}}" {{#if current}} class="active"{{/if}}>{{label}}</a>
            {{/foreach}}
        </div>
</div>
{{else}}

<nav class="navbar fixed-top portfolio-navbar gradient">
    <div class="container">
        <a class="navbar-brand logo" href="{{@site.url}}">{{@site.title}}</a>
        <ul class="nav ml-auto">
            {{#foreach navigation}}
                <li class="nav-item" role="presentation"><a class="nav-link" href="{{url}}">{{label}}</a></li>
            {{/foreach}}
        </ul>
    </div>
</nav>
{{/if}}